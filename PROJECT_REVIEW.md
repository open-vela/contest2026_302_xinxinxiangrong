# 知识闪卡项目审核说明

## ① 项目名称与一句话定位

**项目名称：知镜 KnowLens——xk Knowledge Cards（知识闪卡）**

**一句话定位：**面向学生、程序员和需要碎片化学习的可穿戴设备用户，提供一个运行在圆形智能手表上的语音学习闪卡应用；用户按住麦克风说出学习主题，设备将语音识别为文字，匹配本地卡组，并把短知识点按行显示在圆形 LCD 上，解决小屏设备上长文本难阅读、手动输入不方便的问题。

当前版本内置 CSAPP（《深入理解计算机系统》）离线卡组，支持“计算机系统”“CSAPP”等主题。任意主题的云端大模型生成卡组属于后续扩展方向，当前版本不把它作为已交付能力。

## ② 目标开发板及选板理由

**目标开发板：恒玄 BES2800BP 开发板（OpenVela board id：`best1700_ep`，project：`aos_evb`）。**

选择理由如下：

- 板载 454×454 圆形 LCD 和触控输入，适合验证圆形穿戴设备上的卡片排版、逐行阅读和滑动翻页。
- 提供 OpenVela Media 音频采集链路，可连接板载或外接麦克风，完成 16 kHz、16 bit、单声道 PCM 录音。
- 集成 WiFi 能力，可访问火山引擎语音识别服务；BES2800BP 的多核处理器和硬件资源能够承担 LVGL UI、录音缓存、网络 TLS 和异步识别任务。
- 目标系统是 NuttX/OpenVela，项目可以直接使用 LVGL、Media、mbedTLS、配置存储等系统能力，适合验证端云协同的可穿戴学习场景。

本项目的核心交互是“麦克风 + WiFi + 圆形 LCD + 触控”，当前不需要摄像头、PWM 电机或舵机等额外外设。

## ③ 核心功能与所需硬件能力

| 核心功能 | 功能说明 | 所需硬件能力 |
| --- | --- | --- |
| 圆形学习卡片界面 | 在 454×454 圆屏安全区内显示主题、标题、正文和页码，适配圆形屏幕边缘，避免文字被裁切。 | LCD/DSI 显示链路；无需摄像头、PWM。 |
| 逐行阅读 | 卡片正文按行逐步出现，用户可以点击卡片立即展开，降低小屏阅读压力。 | LCD、CPU 定时器和 RAM 文本缓冲；无需额外传感器。 |
| 卡片翻页与触控交互 | 通过左右导航按钮或左右滑动切换卡片，点击卡片展开全文。 | 触摸屏/触控输入设备 `/dev/input0`；LCD。 |
| CSAPP 离线卡组 | 内置计算机系统、数据表示、编译过程、存储层次、虚拟内存、并发等卡片，断网也能阅读。 | Flash/代码存储、RAM；LCD；无需网络和摄像头。 |
| 按住说话、松开识别 | 长按圆屏底部麦克风按钮开始录音，松开后停止录音并提交识别，界面显示录音、识别和错误状态。 | 麦克风、音频采集接口和 Media 音频服务；16 kHz/16 bit/单声道 PCM。 |
| 云端语音识别 | 将录制的主题语音发送到火山引擎 ASR，识别结果回传后匹配本地卡组。 | WiFi 网络、DNS、TLS 网络连接、麦克风；不需要摄像头。 |
| 服务配置与调试 | 通过 NuttX NSH 配置 App ID/Token，查询 ASR 配置状态并查看错误日志。 | UART/串口用于调试和配置；网络用于 ASR。 |

## ④ 各功能技术方案

### 端侧 UI 与卡片阅读

- 应用使用 C 编写，作为 NuttX/OpenVela 用户态应用 `knowledge_cards` 集成到 `packages/demos/knowledge_cards`。
- 使用 LVGL 创建圆形屏幕 UI：固定 336 px 内容卡片、内收导航按钮、顶部主题状态和底部麦克风按钮；通过 LVGL Timer 控制正文逐行显示。
- 卡片内容当前以 C 常量编译进固件，属于端侧离线数据，不依赖云端模型；自定义中文字体以 C 字节数组编译进应用，保证常用中文主题和正文可显示。
- 触控通过 OpenVela/NuttX LVGL touchscreen driver 接入，支持点击、滑动和按钮事件。

### 端侧录音与异步任务

- 使用 OpenVela Media 音频采集能力及 `audio_capture` 接口打开麦克风，配置为 16 kHz、16 bit、单声道。
- 录音和识别运行在后台 pthread worker 中，LVGL 主线程只轮询状态并刷新 UI，避免网络请求阻塞屏幕刷新。
- PCM 音频暂存于端侧 RAM，松开按钮或达到缓冲上限后停止采集并进入识别状态。

### 云端语音识别

- 使用 OpenVela AI Agent 中的 `volc_asr` 实现，通过 mbedTLS 建立 TLS 连接，再使用火山引擎 WebSocket 二进制协议提交 PCM 音频并解析识别结果。
- App ID、Token 和 ASR 集群通过 `knowledge_cards --set-asr` 写入设备配置存储，源码和固件不固化密钥。
- 识别结果在端侧做主题归一化和匹配；当前支持 CSAPP 及其中文别名，未缓存的主题会提示用户“该主题尚未缓存”。

### 网络与系统集成

- WiFi 由 OpenVela 系统网络栈负责连接和 DNS；应用只负责检查网络可达性和调用 TLS/ASR 客户端。
- 使用 OpenVela 的 LVGL、Media、mbedTLS、cJSON/配置存储和 NuttX pthread/semaphore 等系统组件，不引入摄像头、PWM 或独立 Linux 服务。

### 后续扩展

后续可在现有“语音主题 → 卡片展示”链路上增加云端 LLM 卡组生成、端侧缓存和更多学科卡组；这部分需要增加云端 API 适配、JSON 卡片解析和持久化存储，当前不计入已完成核心功能。
